create database ds0;
create database ds1;

create table TF_B_ORDER_0
(
   ORDER_ID             varchar(32) not null,
   USER_ID              varchar(32) not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_1
(
   ORDER_ID             varchar(32) not null,
   USER_ID              varchar(32) not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_2
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_3
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_4
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_5
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_6
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_7
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_8
(
   ORDER_ID             varchar(32) not null,
   USER_ID              varchar(32) not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_9
(
   ORDER_ID             varchar(32) not null,
   USER_ID              varchar(32) not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_10
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_11
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_12
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_13
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_14
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);

create table TF_B_ORDER_15
(
   ORDER_ID             BIGINT not null,
   USER_ID              BIGINT not null,
   STATUS               char(1),
   ORDER_PRICE          numeric(20,2),
   PAY_TYPE             char(2),
   ORDER_ADDRESS        varchar(128),
   ORDER_DISCOUNT       numeric(20,2),
   ORDER_PAY            numeric(20,2),
   CREATE_TIME          timestamp,
   UPDATE_TIME          timestamp,
   primary key (ORDER_ID)
);s